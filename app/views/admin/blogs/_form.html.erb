<%= form_for(admin_blog, multipart: true) do |f| %>
  <% if admin_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_blog.errors.count, "error") %> prohibited this admin_blog from being saved:</h2>

      <ul>
      <% admin_blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category %>
    <%= f.collection_select :category_id, Admin::Category.all, :id, :name, required: :true %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
